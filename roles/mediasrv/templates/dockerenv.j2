PUID="{{ remote_user.uid }}"
PGID="{{ remote_user.gid }}"
TZ="{{ timezone }}"
SECRETSDIR="{{ dockersecretsdir }}"
APPDATADIR="{{ dockerappdatadir }}"
MEDIADIR="{{ mediadir }}"
DLDIR="{{ dldir }}"
SERVER_IP="{{ lan0.ip }}"
